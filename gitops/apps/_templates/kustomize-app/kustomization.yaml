apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: my-app

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Optional: ConfigMap and Secret generators
# configMapGenerator:
#   - name: my-app-config
#     literals:
#       - LOG_LEVEL=info
#       - API_URL=https://api.${CLUSTER_DOMAIN:-example.com}

# Optional: Common labels for all resources
commonLabels:
  app.kubernetes.io/name: my-app
  app.kubernetes.io/part-of: my-app
