apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # FluxCD components (generated by flux bootstrap)
  - gotk-components.yaml
  - gotk-sync.yaml
  # Cluster-specific settings and deployments
  - ../cluster-settings.yaml
  - ../infrastructure.yaml
  - ../apps.yaml
